#include "main.h"
/**
* _isupper - checks for uppercase character
* @c: character to return 1
* Return: Otherwise 0.
*/
int _isupper(int c)
{
if (c >= 'A' && c <= 'Z')
{
return (1);
}
else
{
return (0);
}
}
